import React, {Component} from 'react';

const MyLayer = class MyLayer extends Component {

    render() {
        return <p>Not much of a component, but it's a start</p>
    }
}

export default MyLayer;